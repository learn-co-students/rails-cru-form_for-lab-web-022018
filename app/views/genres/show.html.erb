<h1><%= @genre.name %></h1>
<ul>
  <% @genre.songs.each do |song| %>
    <li><%= song.name %></li>
  <% end %>
</ul>
<ul>
  <% @genre.artists.each do |artist| %>
    <li><%= artist.name %></li>
  <% end %>
</ul>
